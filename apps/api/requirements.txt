fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sse-starlette>=1.8.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
httpx>=0.27.0
qdrant-client>=1.7.0
python-multipart>=0.0.9

# ── ML stack ─────────────────────────────────────────────────────
# Intel Macs cannot install torch > 2.2.2 (no x86_64 wheels upstream).
# Pin the whole chain so `pip install` doesn't pull incompatible
# newer versions on any platform.
numpy<2
torch==2.2.2; platform_system == "Darwin" and platform_machine == "x86_64"
torch>=2.2.2; platform_system != "Darwin" or platform_machine != "x86_64"
transformers==4.44.2
sentence-transformers==3.0.1
huggingface_hub<0.25
